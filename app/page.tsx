'use client';
import { useState, useEffect } from 'react';
import { Tabs,TabsList,TabsTrigger,TabsContent } from '@/components/ui/tabs';
import { Button } from '@/components/ui/button';
import { flushSync } from 'react-dom';
export default function Home(){
 const [saved,S]=useState(false),[fixed,F]=useState(false),[tab,T]=useState('a'),[answer,A]=useState(''),[version,V]=useState(0),[ignore,I]=useState(false);
 useEffect(()=>{const ctx=(document as unknown as {modelContext?:{registerTool:(tool:unknown,options:unknown)=>unknown}}).modelContext;if(!ctx)return;const control=new AbortController();try{Promise.resolve(ctx.registerTool({name:'navigate_memory_demo',description:'打开记忆演示中的一个会话，不确认或修改记忆。',inputSchema:{type:'object',properties:{session:{type:'string',enum:['a','b','c']}},required:['session'],additionalProperties:false},annotations:{readOnlyHint:false},execute:(input:unknown)=>{const data=input as {session?:unknown};if(!data||Object.keys(data).length!==1||!['a','b','c'].includes(String(data.session)))throw new Error('session 必须是 a、b 或 c');flushSync(()=>T(String(data.session)));return {session:data.session};}},{signal:control.signal})).catch(()=>{});}catch{}return()=>control.abort()},[]);
 const rate=fixed?'68%':'80%';
 function generate(){A(saved&&!ignore?`结论：续约表现仍需结合样本与口径判断。据${fixed?'9 月 8 日纠正记录':'9 月 1 日访谈'}，澄海科技试点客户续约率为 ${rate}。服务响应可能影响续约仍是待验证假设，不能据此认定因果。下一步应核对样本量、统计周期并补充客户访谈。`:'没有可用于本次摘要的已确认项目记忆。请先确认访谈记录，或提供本次需要使用的材料。');V(saved&&!ignore?(fixed?2:1):0)}
 return <main><header><div className="brand">知序 <span>研究记忆工作台</span></div><span>● 个人项目 · 交互演示</span></header><div className="workspace"><section><div className="eyebrow">PROJECT 01 / 澄海科技</div><h1>让研究，接着上次继续。</h1><p className="lead">记录有出处，调用有依据，变化有迹可循。</p><p className="notice">虚构案例 · 固定脚本模拟调用 · 状态仅保留在当前页面，刷新重置。未接入真实模型或云端记忆。</p><Tabs value={tab} onValueChange={v=>T(String(v))}><TabsList className="steps"><TabsTrigger value="a">01 建立记忆</TabsTrigger><TabsTrigger value="b">02 新会话调用</TabsTrigger><TabsTrigger value="c">03 纠正与复用</TabsTrigger></TabsList><TabsContent value="a"><article><small>会话 A · 2026 / 09 / 01</small><h2>一次访谈，三种信息</h2><div className="bubble">今天访谈提到，澄海科技试点客户续约率为 80%。我猜服务响应可能影响续约。请记住，我的研究摘要要先写结论。</div><p>识别出一项数值陈述、一项研究假设和一项明确偏好。</p><div className="card"><label>待确认 · 重要数值与研究假设</label><h3>试点客户续约率 80%</h3><p>确认只代表记录符合你的意思，不证明访谈数据真实。</p><Button disabled={saved} onClick={()=>S(true)}>{saved?'✓ 已确认保存':'确认记录内容'}</Button></div><p className="muted">假设：服务响应可能影响续约 · 一并确认<br/>偏好：摘要先写结论 · 用户明确要求，已应用</p><Button variant="outline" onClick={()=>T('b')}>进入新会话 →</Button></article></TabsContent><TabsContent value="b"><article><small>会话 B · 2026 / 09 / 05</small><h2>无需重新交代背景</h2><div className="bubble">帮我写一段澄海科技项目摘要。</div><p>新会话没有附带旧对话，将根据项目范围查找有效记忆。</p><Button onClick={generate}>生成项目摘要</Button>{answer&&<div className="answer" aria-live="polite"><label>已应用：结论优先</label><p>{answer}</p>{version>0&&<a href="#evidence">查看依据 M-01 · v{version} ↗</a>}{fixed&&version===1&&<p className="warning">此摘要依赖已替代的 v1，需重新生成。</p>}</div>}<p><Button variant="outline" onClick={()=>T('c')}>发现数据有误？演示纠正 →</Button></p></article></TabsContent><TabsContent value="c"><article><small>会话 C · 2026 / 09 / 08</small><h2>纠正一次，后续使用新版本</h2><div className="bubble">上次同一批试点客户的续约率记错了，应该是 68%，不是 80%。请更正。</div><div className="revision"><div><small>旧版本 · v1</small><strong>80%</strong></div><span>→</span><div><small>{fixed?'当前有效':'待确认'} · v2</small><strong>68%</strong></div></div><p>替代相同口径的旧记录，并标记依赖旧数字的摘要。历史文本不会被静默改写。</p><Button disabled={!saved||fixed} onClick={()=>F(true)}>{fixed?'✓ 新版本已生效':'确认替代旧版本'}</Button>{!saved&&<p className="warning">请先在「建立记忆」中确认原始记录。</p>}{fixed&&<div className="answer"><p>v1 已退出当前调用。{version===1?'旧摘要已标记需复核。':'目前没有依赖旧版的摘要。'}</p><Button variant="outline" onClick={()=>{I(false);T('b')}}>回到新会话，再次生成 →</Button></div>}</article></TabsContent></Tabs></section><aside id="evidence"><h2>记忆依据</h2><div className="card"><label>M-01 · v{fixed?2:1} / {saved?'已确认':'待确认'}</label><h3>试点客户续约率</h3><div className="number">{rate}</div><p>范围：澄海科技 / 同一批试点客户</p><details open><summary>来源与调用理由</summary><blockquote>{fixed?'“上次同一批试点客户的续约率记错了，应该是 68%，不是 80%。”':'“今天访谈提到，澄海科技试点客户续约率为 80%。”'}</blockquote><p>{fixed?'会话 C · 9 月 8 日 · 用户纠正':'会话 A · 9 月 1 日 · 用户转述访谈'}</p><p>项目与摘要匹配；{saved?'记录已确认。':'尚未确认，不进入跨会话调用。'}</p></details>{saved&&<Button variant="outline" onClick={()=>I(!ignore)}>{ignore?'恢复本次调用':'本次忽略项目记忆'}</Button>}{ignore&&<p className="warning">下次生成不使用项目记忆。</p>}</div><div className="card"><label>M-02 · 研究假设</label><h3>服务响应可能影响续约</h3><p>{saved?'保持假设语气，不得写为已证实因果。':'等待与访谈记录一并确认。'}</p></div><div className="card"><label>P-01 · 明确偏好</label><h3>摘要先写结论</h3><p>用户明确要求 · 本次演示已应用</p></div>{fixed&&<details className="card"><summary>版本轨迹</summary><p>v1 · 80% · 已替代，仅供追溯</p><p>v2 · 68% · 当前有效</p><p>修订者：用户 / 原因：同口径数字更正</p></details>}<p className="muted">演示不包含生产级权限、持久化、删除与团队共享；完整设计见配套说明。</p></aside></div><footer>知序 / 长期记忆 AI 助理<span>从记住，到可信地使用。</span></footer></main>
}

